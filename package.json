{
  "name": "rn-rnw-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "web:dev": "yarn workspace @mono/web dev",
    "web:build": "yarn workspace @mono/web build",
    "web:start": "yarn workspace @mono/web start",
    "mobile:start": "yarn workspace @mono/mobile start",
    "mobile:ios": "yarn workspace @mono/mobile ios",
    "mobile:android": "yarn workspace @mono/mobile android",
    "test": "jest . --silent"
  },
  "devDependencies": {
    "@testing-library/dom": "^7.21.4",
    "@testing-library/jest-dom": "^5.11.1",
    "@testing-library/react": "^10.4.7",
    "@testing-library/react-hooks": "^3.4.1",
    "@types/jest": "^26.0.5",
    "babel-jest": "^26.1.0",
    "eslint": "^7.5.0",
    "eslint-config-seegno": "^15.0.0",
    "jest": "^26.1.0"
  }
}
